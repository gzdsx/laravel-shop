import App from './App';

// const routes = require('./routes');
// const router = new VueRouter({
//     routes
// });

new Vue({
    el: "#app",
    render: function (h) {
        return h(App);
    }
});
